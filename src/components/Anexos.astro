---
---

<section id="anexos" class="section bg-gray-900">
  <div class="container mx-auto px-4">
    <h2 class="section-title">5. Anexos</h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <!-- Visualización de datos -->
      <div class="card">
        <h3 class="text-xl font-semibold mb-4 text-green-400">5.1. Visualización de Datos</h3>
        
        <!-- Placeholder for future waveform visualization -->
        <div class="relative h-48 bg-black/40 rounded-lg overflow-hidden mb-4 border border-gray-700 flex items-center justify-center">
          <p class="text-gray-400">Visualización de Forma de Onda (Próximamente)</p>
        </div>
        
        <p class="text-gray-300">
          Las visualizaciones de ondas sonoras y espectrogramas son herramientas fundamentales 
          para el análisis acústico. En esta sección se incluirán muestras representativas 
          de vocalizaciones de diferentes especies de primates analizadas.
        </p>
      </div>
      
      <!-- Instrumentos de recolección -->
      <div class="card">
        <h3 class="text-xl font-semibold mb-4 text-green-400">5.2. Instrumentos de Recolección</h3>
        
        <div class="grid grid-cols-2 gap-4 mb-4">
          <div class="bg-black/30 p-3 rounded-lg border border-gray-800">
            <h4 class="text-green-300 font-medium text-sm mb-1">Grabador Autónomo</h4>
            <div class="h-24 bg-black/40 rounded-md flex items-center justify-center">
              <span class="text-xs text-gray-500">Imagen próximamente</span>
            </div>
          </div>
          
          <div class="bg-black/30 p-3 rounded-lg border border-gray-800">
            <h4 class="text-green-300 font-medium text-sm mb-1">Micrófono Direccional</h4>
            <div class="h-24 bg-black/40 rounded-md flex items-center justify-center">
              <span class="text-xs text-gray-500">Imagen próximamente</span>
            </div>
          </div>
        </div>
        
        <p class="text-gray-300">
          Los dispositivos de grabación pasiva se despliegan en sitios estratégicos del bosque amazónico, 
          programados para capturar audio continuamente o durante períodos específicos.
        </p>
      </div>
      
      <!-- Código de la implementación -->
      <div class="card col-span-1 md:col-span-2">
        <h3 class="text-xl font-semibold mb-4 text-green-400">5.3. Implementación del Modelo</h3>
        
        <div class="bg-black/40 p-4 rounded-lg mb-4 font-mono text-sm overflow-x-auto">
          <pre class="text-green-300"><code># Ejemplo de código para el modelo de clasificación
import tensorflow as tf
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Conv2D, MaxPooling2D, Dense, Dropout, Flatten
from tensorflow.keras.optimizers import Adam

# Definición del modelo CNN para clasificación de espectrogramas
def create_primate_acoustic_model(input_shape=(128, 128, 3), num_classes=10):
    model = Sequential([
        # Primera capa convolucional
        Conv2D(32, kernel_size=(3, 3), activation='relu', input_shape=input_shape),
        MaxPooling2D(pool_size=(2, 2)),
        
        # Segunda capa convolucional
        Conv2D(64, kernel_size=(3, 3), activation='relu'),
        MaxPooling2D(pool_size=(2, 2)),
        
        # Tercera capa convolucional
        Conv2D(128, kernel_size=(3, 3), activation='relu'),
        MaxPooling2D(pool_size=(2, 2)),
        
        # Aplanar y capas densas
        Flatten(),
        Dense(256, activation='relu'),
        Dropout(0.5),  # Prevenir sobreajuste
        Dense(num_classes, activation='softmax')
    ])
    
    # Compilar modelo
    model.compile(
        loss='categorical_crossentropy',
        optimizer=Adam(learning_rate=0.001),
        metrics=['accuracy']
    )
    
    return model

# Crear modelo para 8 especies de primates amazónicos
primate_model = create_primate_acoustic_model(num_classes=8)
print(primate_model.summary())</code></pre>
        </div>
        
        <div class="flex flex-col md:flex-row gap-4">
          <div class="flex-1">
            <h4 class="text-lg font-medium mb-2 text-green-400">Preprocesamiento</h4>
            <p class="text-gray-300 text-sm">
              El código incluye módulos para convertir audio raw en espectrogramas 
              mel-escalados, filtrar ruido ambiental y segmentar vocalizaciones.
              Se implementan técnicas de aumentación para mejorar la robustez del modelo.
            </p>
          </div>
          <div class="flex-1">
            <h4 class="text-lg font-medium mb-2 text-green-400">Entrenamiento</h4>
            <p class="text-gray-300 text-sm">
              El modelo se entrena con validación cruzada, early stopping y 
              ajuste de learning rate. Se implementan callbacks para guardar 
              el mejor modelo y visualizar el progreso del entrenamiento.
            </p>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Ethical considerations -->
    <div class="mt-10 card">
      <h3 class="text-xl font-semibold mb-4 text-green-400">5.4. Consideraciones Éticas</h3>
      
      <p class="text-gray-300 mb-4">
        La investigación con monitoreo acústico pasivo proporciona ventajas éticas significativas, minimizando la perturbación 
        a los primates y su hábitat. Sin embargo, se consideran los siguientes aspectos:
      </p>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div class="bg-black/20 p-4 rounded-lg border border-gray-800">
          <h4 class="text-green-300 font-medium mb-2">Privacidad de datos</h4>
          <p class="text-sm text-gray-400">
            Los datos recolectados son utilizados exclusivamente con fines científicos, con medidas para 
            prevenir su uso indebido en actividades como la caza furtiva o el tráfico de especies.
          </p>
        </div>
        
        <div class="bg-black/20 p-4 rounded-lg border border-gray-800">
          <h4 class="text-green-300 font-medium mb-2">Comunidades locales</h4>
          <p class="text-sm text-gray-400">
            Se respeta el conocimiento y derechos de las comunidades indígenas sobre el territorio, 
            integrando su participación en el diseño y ejecución de la investigación.
          </p>
        </div>
        
        <div class="bg-black/20 p-4 rounded-lg border border-gray-800">
          <h4 class="text-green-300 font-medium mb-2">Impacto ambiental</h4>
          <p class="text-sm text-gray-400">
            Los dispositivos de grabación se instalan y mantienen minimizando el impacto en el ecosistema. 
            Se utilizan baterías recargables y materiales no contaminantes.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
